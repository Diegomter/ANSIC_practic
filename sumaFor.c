/* fichero sumaFor.c */

#include <stdio.h>

void main(void) {

    int i, suma=0;
    for(i=2; i<=100; i++)
    	printf("%d\n", suma+);
        suma+=i;
    printf("La suma de los cinco primeros numeros es: %d\n", suma);
}

